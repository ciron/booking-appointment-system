import React from 'react';

const AuthenticatePatient = () => {
    return (
        <div className="container mt-5">
            <h1>Welcome, Patient!</h1>
            <p>You are now authenticated and can access the patient dashboard.</p>
        </div>
    );
};

export default AuthenticatePatient;
